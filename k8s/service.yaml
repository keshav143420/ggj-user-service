apiVersion: v1
kind: Service
metadata:
  name: ggj-user-service
  labels:
    app: ggj-user-service
spec:
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
  selector:
    app: ggj-user-service
  type: ClusterIP
